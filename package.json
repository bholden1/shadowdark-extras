{
  "name": "shadowdark-extras",
  "version": "6.10.41",
  "private": true,
  "description": "Foundry VTT module — extras for Shadowdark RPG. Dev tooling only; the module itself ships as a manifest install.",
  "scripts": {
    "verify": "bash verify.sh",
    "verify:strict": "bash verify.sh --strict",
    "pack": "node ./dev/packs/pack.mjs",
    "unpack": "node ./dev/packs/unpack.mjs",
    "release:check": "node ./dev/release-check.mjs"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.0.4"
  },
  "dependencies": {
    "rot-js": "^2.2.1"
  }
}
